# ===========================================
# Om Industries - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values:
#   copy .env.example .env    (Windows)
#   cp .env.example .env      (Linux/Mac)
# ===========================================

# --- REQUIRED for Supabase (Local + Railway) ---
# Get from: Supabase Dashboard → Settings → Database → Connection string
# Use "Session pooler" (port 6543) for Railway / serverless
# Use "URI" (port 5432) if Session pooler has issues
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:6543/postgres?pgbouncer=true

# --- REQUIRED for production (Railway) ---
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-in-production

# --- OPTIONAL: Email (Contact form) ---
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# RECIPIENT_EMAIL=admin@omindustriesindia.com
